<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Testrix">

        <!-- Главный экран — NativeActivity, который загружает твою C++ библиотеку -->
        <activity android:name="android.app.NativeActivity"
            android:exported="true">

            <!-- Указываем имя библиотеки (должно совпадать с project(testrix)) -->
            <meta-data android:name="android.app.lib_name"
                android:value="testrix" />

            <!-- Говорим системе, что это главный экран приложения -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>